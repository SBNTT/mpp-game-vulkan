package = me.sbntt.mppgame.vulkan
headers = vulkan/vulkan.h

compilerOpts = -DVK_NO_PROTOTYPES

---

static inline unsigned int vkMakeVersion(uint32_t major, uint32_t minor, uint32_t patch) {
    return VK_MAKE_VERSION(major, minor, patch);
}